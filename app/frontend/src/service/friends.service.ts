import { IFriends } from '../@types/friends';

export const createFriends = async (friends: IFriends) => {
  // return await this.cardRepository.createCard(userId, card);
  return '';
};
export const search = async (name: string) => {
  // return await this.cardRepository.createCard(userId, card);
  return '';
};
